//class the contains all of the data about a specific problem
#include <string>
#include <vector>
#include <fstream>
#include <iostream>

class alog{
private:
    std::string textFileLocation = "./data/alogNames.txt"; //location of the text file
    std::vector<strings> m_ProblemNames;  //vector with of all of the problem titles
    bool haveNames;
}

public:

  void createSSofNames() //creates a vector with each element containing a problems name;

};
